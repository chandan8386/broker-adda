# Copy the value into Vercel -> Project -> Settings -> Environment Variables
# (Production + Preview). Vite inlines VITE_* at build time.
#
# Must include the /api context path and NO trailing slash.
VITE_API_BASE_URL=https://trishakti-crm-api.onrender.com/api
